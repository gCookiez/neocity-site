{
  "name": "neocity-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite --host --port 8888",
    "dev-generate": "node pre-render/publisher.js && node pre-render/converter.js && vite --host --port 8888",
    "build": "node pre-render/publisher.js && node pre-render/converter.js && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node pre-render/publisher.js && node pre-render/converter.js",
    "upload": "node --env-file=.env ./post-render/upload.js",
    "generate-upload": "node pre-render/publisher.js && node pre-render/converter.js && vite build && node --env-file=.env ./post-render/upload.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "gsap": "^3.15.0",
    "tsc": "^2.0.4",
    "vite": "^8.0.8"
  },
  "dependencies": {
    "fslightbox": "^3.7.5",
    "js-yaml": "^4.1.1",
    "jsdom": "^27.0.1",
    "neocities": "^0.0.3"
  }
}
